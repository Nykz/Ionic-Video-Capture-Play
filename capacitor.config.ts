import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.videoCaptureApp',
  appName: 'Video Capture',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
